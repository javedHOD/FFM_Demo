import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webotiksffmv3.app',
  appName: 'field-force-v3',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
